title: Instructions Processing Response
type: object
properties:
  status:
    type: string
    enum: [ "PROCESSED", "FAILED" ]
    description: Status of the instructions processing
    example: "PROCESSED"
  message:
    type: string
    description: Human-readable result of processing
    example: "10 Files updates successfully"
required:
  - status
  - message
